package com.hsd.fsxasm.activity;import android.content.Intent;import android.view.View;import android.view.View.OnClickListener;import android.widget.EditText;import com.hsd.fsxasm.R;import com.hsd.fsxasm.base.BaseActivity;public class SetSignatureActivity extends BaseActivity {		private EditText signatureText;	private String signature;			@Override	protected View initView() {		View parent = View.inflate(this, R.layout.il_setotherpersoninfo_setsignature, null);		Intent intent = getIntent();		signature = intent.getStringExtra("signature");		signatureText = (EditText) parent.findViewById(R.id.il_setotherpersoninfo_setsignature_signature);		signatureText.setText(signature);				return parent;	}	@Override	protected void updataUI(Object obj) {		// TODO Auto-generated method stub	}	@Override	protected void initData() {		mBack.setVisibility(View.VISIBLE);		mTitle.setText("设置个签");		mHelp.setVisibility(View.VISIBLE);		mHelp.setText("完成");	}	@Override	protected void setListener() {		mBack.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				signature = signatureText.getText().toString();				Intent intent = getIntent();				intent.putExtra("sign324234ature", signature);				setResult(53453, intent);				finish();			}		});		mHelp.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				finish1();			}		});	}	private void finish1(){		signature = signatureText.getText().toString();		Intent intent = getIntent();		intent.putExtra("signature", signature);		this.setResult(3, intent);		finish();	}}