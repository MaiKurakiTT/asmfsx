package com.hsd.fsxasm.activity;import org.xutils.view.annotation.ContentView;import android.content.Intent;import android.view.View;import android.view.View.OnClickListener;import android.widget.EditText;import android.widget.RelativeLayout;import android.widget.Toast;import com.hsd.fsxasm.R;import com.hsd.fsxasm.base.BaseActivity;import com.hsd.fsxasm.domain.LoginBean;import com.hsd.fsxasm.engine.NLogin;import com.hsd.fsxasm.global.SharedPreferencesString;import com.hsd.fsxasm.global.TempInfo;public class LoginActivity extends BaseActivity {	private RelativeLayout login;	private EditText editUserName;	private EditText editPassWorded;	private String userName, passWord;	@Override	protected View initView() {		// TODO Auto-generated method stub		View parents = View.inflate(mActivity, R.layout.il_login, null);		login = (RelativeLayout) parents.findViewById(R.id.but_login_login);		editUserName = (EditText) parents.findViewById(R.id.it_login_username);		editPassWorded = (EditText) parents.findViewById(R.id.it_login_password);		return parents;	}	@Override	protected void updataUI(Object obj) {		// TODO Auto-generated method stub		LoginBean login = (LoginBean) obj;		if(login.getResultCode() == 1){			Intent intent = new Intent(mActivity,MainFragmentActivity.class);			startActivity(intent);			saveConfigInfo(SharedPreferencesString.IS_LOGIN, true);			saveConfigInfo(SharedPreferencesString.CURRENT_USER, editUserName.getText().toString());			TempInfo.init(mActivity, sp.getString(SharedPreferencesString.CURRENT_USER, ""));		}				Toast.makeText(mActivity, login.getDescribe(), Toast.LENGTH_SHORT).show();				setResult(2);		finish();	}	@Override	protected void setListener() {		// TODO Auto-generated method stub		login.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				userName = editUserName.getText().toString();				passWord = editPassWorded.getText().toString();								if(userName.isEmpty() || passWord.isEmpty()){					Toast.makeText(mActivity, "账号或密码不可以为空", Toast.LENGTH_SHORT).show();					return;				}												NLogin login = new NLogin(mActivity, new HttpResponseHandler());				login.login(userName, passWord);			}		});	}	@Override	protected void initData() {		// TODO Auto-generated method stub		mTitle.setText("登录");	}}