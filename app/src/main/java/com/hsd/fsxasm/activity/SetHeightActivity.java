package com.hsd.fsxasm.activity;import android.content.Context;import android.content.Intent;import android.view.LayoutInflater;import android.view.View;import android.view.View.OnClickListener;import android.view.ViewGroup;import android.widget.RelativeLayout;import android.widget.TextView;import com.hsd.fsxasm.R;import com.hsd.fsxasm.base.BaseActivity;import com.hsd.fsxasm.widget.cityanddatewheel.wheelcity.AddressData;import com.hsd.fsxasm.widget.cityanddatewheel.wheelcity.OnWheelChangedListener;import com.hsd.fsxasm.widget.cityanddatewheel.wheelcity.WheelView;import com.hsd.fsxasm.widget.cityanddatewheel.wheelcity.adapters.AbstractWheelTextAdapter;public class SetHeightActivity extends BaseActivity {	private TextView heightText;	private RelativeLayout chooseViewParent;	private View chooseView;	private String height;	private TextView ok;	private TextView close;	private String cityTxt;		@Override	protected View initView() {		View parent = View.inflate(this, R.layout.il_setotherpersoninfo_setheight, null);				Intent intent = getIntent();		heightText = (TextView) parent.findViewById(R.id.id_setotherpersoninfo_setheight_height);		height = intent.getStringExtra("height");		heightText.setText(height);		chooseViewParent = (RelativeLayout) parent.findViewById(R.id.il_setotherpersoninfo_setheight_choose);		chooseViewParent.addView(dialogm());		return parent;	}	@Override	protected void initData() {		mBack.setVisibility(View.VISIBLE);		mHelp.setVisibility(View.VISIBLE);		mTitle.setText("设置身高");		mHelp.setText("完成");	}		@Override	protected void updataUI(Object obj) {		// TODO Auto-generated method stub	}	@Override	public void onBackPressed() {		height = heightText.getText().toString();		Intent intent = getIntent();		intent.putExtra("heigh1t", height);		setResult(11121, intent);		finish();		//		super.onBackPressed();	}	@Override	protected void setListener() {		mBack.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				height = heightText.getText().toString();				Intent intent = getIntent();				intent.putExtra("heigh1t", height);				setResult(11121, intent);				finish();			}		});		mHelp.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				finish1();			}		});	}		private View dialogm() {		View contentView = LayoutInflater.from(this).inflate(				R.layout.wheelcity_height_layout, null);		final WheelView country = (WheelView) contentView				.findViewById(R.id.wheelheight_height);		country.setVisibleItems(3);		country.setViewAdapter(new CountryAdapter(this));		country.addChangingListener(new OnWheelChangedListener() {			public void onChanged(WheelView wheel, int oldValue, int newValue) {//				updateCities(city, cities, newValue);				cityTxt = AddressData.HEIGHT[country.getCurrentItem()];				heightText.setText(cityTxt);			}		});		country.setCurrentItem(50);// 设置北京		return contentView;	}		/**	 * Adapter for countries	 */	private class CountryAdapter extends AbstractWheelTextAdapter {		// Countries names		private String countries[] = AddressData.HEIGHT;		/**		 * Constructor		 */		protected CountryAdapter(Context context) {			super(context, R.layout.wheelcity_height_item_layout, NO_RESOURCE);			setItemTextResource(R.id.wheelheight_height_name);		}		@Override		public View getItem(int index, View cachedView, ViewGroup parent) {			View view = super.getItem(index, cachedView, parent);			return view;		}		@Override		public int getItemsCount() {			return countries.length;		}		@Override		protected CharSequence getItemText(int index) {			return countries[index];		}	}	private void finish1(){		height = heightText.getText().toString();		Intent intent = getIntent();		intent.putExtra("height", height);		this.setResult(11, intent);		finish();	}}