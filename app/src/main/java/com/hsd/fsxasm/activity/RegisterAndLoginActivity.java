package com.hsd.fsxasm.activity;import android.content.Intent;import android.view.View;import android.view.View.OnClickListener;import android.widget.RelativeLayout;import com.hsd.fsxasm.R;import com.hsd.fsxasm.base.BaseActivity;import com.hsd.fsxasm.utils.LogUtil;public class RegisterAndLoginActivity extends BaseActivity{		private RelativeLayout login;	private RelativeLayout register;		@Override	protected void initData() {		// TODO Auto-generated method stub		mTitle.setText("");	}	@Override	protected View initView() {		View parents = View.inflate(mActivity, R.layout.il_loginandregister, null);		login = (RelativeLayout) parents.findViewById(R.id.il_loginandregister_login);		register = (RelativeLayout) parents.findViewById(R.id.il_loginandregister_register);		return parents;	}	@Override	protected void updataUI(Object obj) {		// TODO Auto-generated method stub			}	@Override	protected void setListener() {		// TODO Auto-generated method stub		login.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				LogUtil.info("login", "点击登录");				Intent intent = new Intent(mActivity, LoginActivity.class);				startActivityForResult(intent, 1);			}		});		register.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				LogUtil.info("login", "点击注册");				Intent intent = new Intent(mActivity, CertificationActivity.class);				startActivityForResult(intent, 2);			}		});	}	@Override	protected void onActivityResult(int requestCode, int resultCode, Intent data) {		super.onActivityResult(requestCode, resultCode, data);		switch(resultCode){		case 2:			mFinish();			break;		default:			break;		}	}		private void mFinish(){		finish();	}}