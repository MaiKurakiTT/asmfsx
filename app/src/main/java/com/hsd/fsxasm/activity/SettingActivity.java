package com.hsd.fsxasm.activity;import java.io.File;import org.xutils.x;import org.xutils.ex.DbException;import android.content.Context;import android.content.Intent;import android.view.View;import android.view.View.OnClickListener;import android.widget.RelativeLayout;import com.hsd.fsxasm.R;import com.hsd.fsxasm.base.BaseActivity;import com.hsd.fsxasm.db.MDbUser;import com.hsd.fsxasm.db.MDbUtils;import com.hsd.fsxasm.utils.DeleteFile;public class SettingActivity extends BaseActivity{	private RelativeLayout logoutClick;	@Override	protected View initView() {		View parent = View.inflate(mActivity, R.layout.il_setting, null);		logoutClick = (RelativeLayout) parent.findViewById(R.id.il_setting_logoutclick);		return parent;	}	@Override	protected void initData() {		mBack.setVisibility(View.VISIBLE);		mTitle.setText("设置");	}	@Override	protected void setListener() {		logoutClick.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				/**				 * 注销并退出				 */				  sp = getSharedPreferences("init", Context.MODE_PRIVATE);			        sp.edit().clear().commit();			        sp = getSharedPreferences("config",  Context.MODE_PRIVATE);			       sp.edit().clear().commit();			        try {						x.getDb(MDbUtils.daoConfig).dropTable(MDbUser.class);					} catch (DbException e) {						// TODO Auto-generated catch block						e.printStackTrace();					}			        ///sdcard/asmfsx/image/head.jpg			        DeleteFile deleteFile = new DeleteFile("sdcard/asmfsx/image/head.jpg");			        deleteFile.DeleteFolder();			        setResult(4);			        finish();			        			}		});		mBack.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				finish();			}		});			}		@Override	protected void updataUI(Object obj) {		// TODO Auto-generated method stub			}}