package com.hsd.fsxasm.global;import org.xutils.DbManager;import org.xutils.x;import org.xutils.ex.DbException;import com.hsd.fsxasm.db.MDbString;import com.hsd.fsxasm.db.MDbUser;import com.hsd.fsxasm.db.MDbUtils;import android.app.Activity;public class TempInfo {	private static Activity mActivity;		public static void init(Activity activity,String phone){		mActivity = activity;		DbManager db = x.getDb(MDbUtils.daoConfig);		try {			findFirst = db.selector(MDbUser.class).where(MDbString.phone, "=", phone).findFirst();		} catch (DbException e) {			// TODO Auto-generated catch block			e.printStackTrace();		}	}			public static MDbUser findFirst;	}