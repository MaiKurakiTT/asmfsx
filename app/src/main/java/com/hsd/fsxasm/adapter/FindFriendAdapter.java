package com.hsd.fsxasm.adapter;import java.util.List;import android.app.Activity;import android.view.View;import android.view.ViewGroup;import android.widget.BaseAdapter;import android.widget.ImageView;import android.widget.PopupWindow;import android.widget.TextView;import com.hsd.fsxasm.R;import com.hsd.fsxasm.domain.UserInformationBean;import com.hsd.fsxasm.utils.AsyncImageLoader;import com.hsd.fsxasm.utils.FormatDate2String;import com.hsd.fsxasm.utils.GetAgeFromDate;import com.hsd.fsxasm.widget.PopWindow;public class FindFriendAdapter extends BaseAdapter{	private List<UserInformationBean> beans;		private Activity mActivity;			public FindFriendAdapter(List<UserInformationBean> lists,Activity activity) {		super();		beans = lists;		mActivity = activity;		// TODO Auto-generated constructor stub	}	@Override	public int getCount() {		// TODO Auto-generated method stub		return beans.size();	}	@Override	public Object getItem(int position) {		// TODO Auto-generated method stub		return beans.get(position);	}	@Override	public long getItemId(int position) {		// TODO Auto-generated method stub		return position;	}	@Override	public View getView(int position, View convertView, ViewGroup parent) {		// TODO Auto-generated method stub		Holder holder;		if(convertView==null){			holder = new Holder();			convertView = View.inflate(mActivity, R.layout.id_foufri_itemlay, null);			holder.imgHead = (ImageView) convertView					.findViewById(R.id.id_foufri_head);			holder.tvName = (TextView) convertView					.findViewById(R.id.id_foufri_name);			holder.tvAge = (TextView) convertView					.findViewById(R.id.id_foufri_age);			holder.tvConstellation = (TextView) convertView					.findViewById(R.id.id_foufri_constellation);			holder.tvSex = (TextView) convertView					.findViewById(R.id.id_foufri_sex);			holder.tvSchool = (TextView) convertView					.findViewById(R.id.id_foufri_school);			convertView.setTag(holder);		}else{			holder = (Holder) convertView.getTag();		}		UserInformationBean bean = beans.get(position);		//x.image().bind(holder.imgHead, bean.getUser_icon());		AsyncImageLoader.getInstance().getImageLoaderInstance(mActivity).displayImage(bean.getUser_icon(), holder.imgHead);				holder.tvName.setText(bean.getUser_nickname());//		holder.tvAge.setText("16");		holder.tvAge.setText(GetAgeFromDate.getAge(bean.getUser_birthday())+"岁");		holder.tvConstellation.setText(bean.getUser_star());		holder.tvSex.setText(bean.getUser_sex());		holder.tvSchool.setText(bean.getUser_school());		return convertView;	}	private class Holder{			ImageView imgHead;			TextView tvName;			TextView tvSex;			TextView tvAge;			TextView tvConstellation;			TextView tvSchool;	}}