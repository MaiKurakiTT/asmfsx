package com.hsd.fsxasm.base;import com.hsd.fsxasm.global.Constructs;import com.hsd.fsxasm.utils.NetworkUtils;import com.hsd.fsxasm.widget.loading.ShapeLoadingDialog;import android.app.Activity;import android.content.Context;import android.os.Handler;import android.os.Message;public class BaseEngine {	public BaseEngine() {		super();		// TODO Auto-generated constructor stub	}	protected Activity mActivity;	protected Handler mHandler;		protected ShapeLoadingDialog dialog;	public BaseEngine(Activity mActivity,Handler handler) {		super();		this.mActivity = mActivity;		this.mHandler = handler;		dialog = new ShapeLoadingDialog(mActivity);	}			public BaseEngine(Activity mActivity) {		super();		this.mActivity = mActivity;	}	protected boolean checkNet() {		boolean flag = NetworkUtils.isNetworkAvailable(mActivity);		return flag;	}	}